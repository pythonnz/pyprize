[project]
name = "pyprize"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.112.2",
    "sqlmodel>=0.0.21",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.6.3",
]
